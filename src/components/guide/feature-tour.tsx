"use client";

/**
 * First-run tour disabled — auto-open caused setTimeout delays and
 * extra mount risk during navigation. Guides remain via PageIntro.
 */
export function FeatureTour() {
  return null;
}
